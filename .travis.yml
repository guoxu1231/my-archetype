language: java

jdk:
  - openjdk7
os:
  - linux

install:
  - echo 'install'

before_script:
  - ./ci_script/before_script.sh

script:
  - mvn clean -pl archetype-helloworld test -X

cache:
  directories:
    - $HOME/.m2

services:
  - mysql