language: java

jdk:
  - openjdk7
os:
  - linux

install:
  - echo 'install'

before_script:
  - ./ci_script/before_script.sh

script:
  - mvn clean -pl archetype-helloworld test

after_failure:
  - cat target/surefire-reports/*.txt

cache:
  directories:
    - $HOME/.m2

services:
  - mysql